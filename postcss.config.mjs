/**
 * Konfigurasi PostCSS
 *
 * Tailwind CSS v4 (melalui @tailwindcss/vite) secara otomatis mengatur semua
 * plugin PostCSS yang diperlukan — kamu TIDAK perlu menyertakan `tailwindcss`
 * atau `autoprefixer` di sini.
 *
 * File ini hanya ada untuk menambahkan plugin PostCSS tambahan, jika diperlukan.
 * Contohnya:
 *
 * import postcssNested from 'postcss-nested'
 * export default { plugins: [postcssNested()] }
 *
 * Jika tidak diperlukan, kamu bisa membiarkan file ini kosong.
 */
export default {}